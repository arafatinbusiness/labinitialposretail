rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow ALL operations for authenticated users
    // This is the simplest approach for better user experience
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
