# বাংলা ফন্ট ইনস্টলেশন গাইড - Bengali Font Installation Guide

## সমস্যা: বাংলা টেক্সট ভাঙ্গা দেখায় (Joint Letters Breaking)
**Problem:** Bengali text appears broken, joint letters (যুক্তাক্ষর) don't render properly in browsers and applications.

## সমাধান: SutonnyMJ ফন্ট ইনস্টল করুন
**Solution:** Install SutonnyMJ font system-wide for proper Bengali rendering.

## 📦 ইনস্টলেশন স্টেপস - Installation Steps

### 1. স্ক্রিপ্ট ডাউনলোড করুন - Download the Script
```bash
# Make sure you're in the project directory
cd /mnt/ssd1/AndroidStudioProjects/Labinitial_Business_management
```

### 2. স্ক্রিপ্ট এক্সিকিউটেবল করুন - Make Script Executable
```bash
chmod +x install-bengali-font.sh
```

### 3. রুট হিসেবে রান করুন - Run as Root
```bash
sudo ./install-bengali-font.sh
```

## 🚀 স্ক্রিপ্ট কি করবে - What the Script Does

### 1. ফন্ট ইনস্টলেশন - Font Installation
- **সিস্টেম-ওয়াইড**: `/usr/share/fonts/truetype/SutonnyMJ/`
- **ইউজার লেভেল**: `~/.local/share/fonts/`

### 2. ফন্ট ক্যাশে আপডেট - Font Cache Update
```bash
fc-cache -f -v  # System cache
sudo -u $USER fc-cache -f -v  # User cache
```

### 3. ফন্টকনফিগ কনফিগারেশন - Fontconfig Configuration
Creates: `/etc/fonts/conf.d/65-bengali-sutonny.conf`
- বাংলা টেক্সটের জন্য SutonnyMJ ফন্ট প্রাথমিকভাবে ব্যবহার করবে
- যুক্তাক্ষর সঠিকভাবে রেন্ডার করবে

### 4. অতিরিক্ত বাংলা ফন্ট - Additional Bengali Fonts
Installs (if apt available):
- `fonts-beng` - Standard Bengali fonts
- `fonts-noto-bengali` - Google Noto Bengali
- `fonts-lohit-beng-bengali` - Lohit Bengali

## 🔍 টেস্টিং - Testing

### 1. টেস্ট পেজ ওপেন করুন - Open Test Page
```bash
xdg-open /tmp/bengali-test.html
```

### 2. ফন্ট ডিটেকশন চেক করুন - Check Font Detection
```bash
fc-list | grep -i sutonny
```

### 3. স্যাম্পল টেক্সট টেস্ট - Sample Text Test
```bash
echo "বাংলাদেশের রাজধানী ঢাকা। বাংলা ভাষা বাংলাদেশের রাষ্ট্রভাষা।"
```

## 🖥️ ব্রাউজার কনফিগারেশন - Browser Configuration

### Firefox:
1. **about:config** এ যান
2. **font.name.serif.bn** → `SutonnyMJ`
3. **font.name.sans-serif.bn** → `SutonnyMJ`
4. **font.name.monospace.bn** → `SutonnyMJ`

### Chrome/Chromium:
1. **Settings** → **Appearance** → **Customize fonts**
2. **Standard font** → `SutonnyMJ`
3. **Serif font** → `SutonnyMJ`
4. **Sans-serif font** → `SutonnyMJ`

## 🔧 ট্রাবলশুটিং - Troubleshooting

### যদি বাংলা টেক্সট এখনও ভাঙ্গা দেখায়:
**If Bengali text still appears broken:**

#### 1. ফন্ট ক্যাশে ক্লিয়ার করুন - Clear Font Cache
```bash
sudo fc-cache -f -v
rm -rf ~/.cache/fontconfig
```

#### 2. ব্রাউজার ক্যাশে ক্লিয়ার করুন - Clear Browser Cache
- **Firefox**: Ctrl+Shift+Delete → Select "All time" → Clear
- **Chrome**: Ctrl+Shift+Delete → Time range "All time" → Clear data

#### 3. সিস্টেম রিস্টার্ট করুন - Restart System
```bash
sudo reboot
```

#### 4. ম্যানুয়ালি ফন্ট চেক করুন - Manually Check Font
```bash
# Check if font is installed
fc-match "SutonnyMJ"

# List all Bengali fonts
fc-list | grep -i bengali

# Test with specific text
echo -e "যুক্তাক্ষর টেস্ট:\nক্ষ্ম হ্ণ হ্ন হ্ম ক্ত স্ক স্ফ স্ন স্ম"
```

## 📱 অ্যাপ্লিকেশন-স্পেসিফিক সেটিংস - Application-Specific Settings

### LibreOffice:
1. **Tools** → **Options** → **LibreOffice** → **Fonts**
2. **Default fonts** → Set SutonnyMJ for all categories
3. **Apply to all documents**

### GIMP:
1. **Edit** → **Preferences** → **Tool Options**
2. **Text Tool** → Default font → `SutonnyMJ`

### Terminal:
```bash
# Set font in terminal preferences
# Look for "SutonnyMJ" or "Monospace" with Bengali support
```

## ✅ ভেরিফিকেশন চেকলিস্ট - Verification Checklist

- [ ] স্ক্রিপ্ট সফলভাবে রান হয়েছে
- [ ] ফন্ট সিস্টেমে ডিটেক্ট হচ্ছে: `fc-list | grep sutonny`
- [ ] টেস্ট পেজে বাংলা টেক্সট স্পষ্ট দেখাচ্ছে
- [ ] যুক্তাক্ষর ভাঙ্গা ছাড়া দেখাচ্ছে (ক্ষ্ম, হ্ণ, হ্ন, etc.)
- [ ] ব্রাউজারে বাংলা ওয়েবসাইট সঠিক দেখাচ্ছে
- [ ] অন্যান্য অ্যাপ্লিকেশনে বাংলা কাজ করছে

## 🎯 এক্সপেক্টেড রেজাল্ট - Expected Result

### ইনস্টলেশনের পর - After Installation:
```
✅ বাংলা টেক্সট স্পষ্ট দেখাবে
✅ যুক্তাক্ষর ভাঙ্গবে না
✅ সব অ্যাপ্লিকেশনে কাজ করবে
✅ ব্রাউজারে সঠিকভাবে রেন্ডার হবে
```

### টেস্ট টেক্সট - Test Text (Should appear correctly):
```
বাংলাদেশ দক্ষিণ এশিয়ার একটি রাষ্ট্র।
যুক্তাক্ষর: ক্ষ্ম হ্ণ হ্ন হ্ম ক্ত স্ক স্ফ স্ন স্ম
আমার সোনার বাংলা, আমি তোমায় ভালোবাসি।
```

## 📞 সাপোর্ট - Support

### সমস্যা হলে - If you have issues:
1. স্ক্রিপ্ট আউটপুট চেক করুন
2. ফন্ট ক্যাশে ক্লিয়ার করুন
3. সিস্টেম রিস্টার্ট করুন
4. ব্রাউজার রিস্টার্ট করুন

### লগ ফাইল - Log Files:
```bash
# Font installation logs
journalctl -u fontconfig

# User font cache
ls -la ~/.cache/fontconfig/

# System font directory
ls -la /usr/share/fonts/truetype/SutonnyMJ/
```

## 🔄 ব্যাকআপ ও রিস্টোর - Backup & Restore

### ব্যাকআপ - Backup:
```bash
# Backup current font configuration
tar -czf font-backup-$(date +%Y%m%d).tar.gz \
    /etc/fonts/ \
    ~/.fonts/ \
    ~/.local/share/fonts/
```

### রিস্টোর - Restore:
```bash
# Extract backup
tar -xzf font-backup-YYYYMMDD.tar.gz -C /
# Update font cache
fc-cache -f -v
```

## 🏁 শেষ কথা - Final Notes

1. **রিস্টার্ট জরুরি**: ইনস্টলেশনের পর কম্পিউটার রিস্টার্ট করুন
2. **ব্রাউজার রিস্টার্ট**: সব ব্রাউজার বন্ধ করে আবার ওপেন করুন
3. **টেস্ট করুন**: বিভিন্ন বাংলা ওয়েবসাইট ভিজিট করুন
4. **ভেরিফাই করুন**: যুক্তাক্ষর সঠিক কিনা চেক করুন

**সফল ইনস্টলেশনের পর আপনার সব বাংলা টেক্সট স্পষ্ট এবং যুক্তাক্ষর ভাঙ্গা ছাড়া দেখাবে।**
**After successful installation, all your Bengali text will appear clear without broken joint letters.**
